package com.kutysam.androidprofiler;



public class Garbage {
/*
 * 			Parcel test = Parcel.obtain();
			test.unmarshall(b4data, 0, b4data.length);
			test.setDataPosition(0);
			Log.e(TAG,"PARCEL INT" + test.readInt() + " " + (int)0xBA757475);
			ProcessStats mProcessStats = new ProcessStats(false);
 */
}
/*
private long getOverallCpuTime(long[] in){
	long temp = 0;
	for(int i = 0; i < PowerValues.cpuSpeedSteps; i ++){
		temp += in[i];
	}
	return temp;
}
/*
//Calculates the total CPUPower consumed in JOULES
 * 
public double calculateOverallCPUPower(UIDProcess p){
	p.totalPower = 0;
	for(int i = 0; i < PowerValues.cpuSpeedSteps; i++){
		p.totalPower += PowerCalculator.calculateJoule(p.totalCpuSpeedStepTimes[i], PowerValues.cpuactives[i], PowerValues.battVoltage);
	}
	return p.totalPower;
}*/